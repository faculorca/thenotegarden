filters:
  and:
    - '!file.name.contains("Template")'
views:
  - type: table
    name: Table
  - type: table
    name: Mentioned
    filters:
      and:
        - file.hasLink(this)
    sort:
      - property: file.name
        direction: ASC
